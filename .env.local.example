# Google Apps Script web app endpoint that receives the batched survey JSON.
# IMPORTANT: the client posts with Content-Type: text/plain;charset=utf-8 to
# avoid the CORS preflight that Apps Script web apps don't answer. Apps Script
# still parses the body as JSON.
NEXT_PUBLIC_SUBMIT_URL=
